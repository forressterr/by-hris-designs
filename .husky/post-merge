# Re-install when a merge or pull changes package.json or package-lock.json.
# ORIG_HEAD = pre-merge HEAD, HEAD = post-merge HEAD.
if [ -n "$(git diff --name-only ORIG_HEAD HEAD -- package.json package-lock.json)" ]; then
  echo "📦 Dependencies changed — running npm install..."
  npm install
fi
